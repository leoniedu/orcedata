% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_distancias_osrm.R
\name{get_distancias_osrm}
\alias{get_distancias_osrm}
\title{Compute OSRM Distance Matrix with Snap Correction}
\usage{
get_distancias_osrm(
  src,
  dst = NULL,
  kmh_snap = 2,
  chunk_size = 5000L,
  preencher_na = FALSE
)
}
\arguments{
\item{src}{An \code{sf} POINT object with origin points.}

\item{dst}{An \code{sf} POINT object with destination points. If \code{NULL} (default),
computes a src-to-src matrix.}

\item{kmh_snap}{Speed in km/h assumed for the snap distance (off-road
portion). Default: \code{2}. This converts the snap distance to additional
travel time. A low value penalizes locations far from roads.}

\item{chunk_size}{Number of origins per OSRM request. Default: \code{5000L}.}

\item{preencher_na}{If \code{TRUE}, fill unroutable pairs with Euclidean
distance at \code{kmh_snap} speed. If \code{FALSE} (default), preserve \code{NA}s.}
}
\value{
A \code{data.frame} with columns from \code{src} and \code{dst} (dropped geometry),
plus:
\describe{
\item{.id_orig}{Integer index of the origin point in \code{src}.}
\item{.id_dest}{Integer index of the destination point in \code{dst}.}
\item{distancia_km}{Total distance: road + snap (origin + destination).}
\item{duracao_horas}{Total duration: road + snap time at \code{kmh_snap}.}
\item{snap_km_orig}{Snap distance for origin point (km).}
\item{snap_km_dest}{Snap distance for destination point (km).}
\item{metodo}{\code{"osrm"} for OSRM-routed pairs, \code{"euclidiano"} for
Euclidean-filled pairs (when \code{preencher_na = TRUE}), or \code{NA} for
unroutable pairs (when \code{preencher_na = FALSE}).}
}

The returned data.frame carries three attributes used by
\code{\link[=completar_distancias]{completar_distancias()}}: \code{"snap_src"} and \code{"snap_dst"} (snapped \code{sf}
points) and \code{"kmh_snap"} (the speed used).
}
\description{
Computes road distances and durations between points using OSRM, with
correction for snap distances (the gap between input coordinates and the
nearest road in the network).
}
\details{
OSRM snaps input coordinates to the nearest point on the road network.
The road distance returned by the OSRM table API only covers the
road-to-road portion. This function adds the snap distances (origin +
destination) to both distance and duration, using \code{kmh_snap} to convert
snap distance to travel time.
}
\seealso{
\code{\link[=completar_distancias]{completar_distancias()}} to complete NA pairs via augmented graph.
}
