% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osrm_local.R
\name{osrm_local_start}
\alias{osrm_local_start}
\title{Start a Local OSRM Server}
\usage{
osrm_local_start(region_pbf, max_table_size = 10000L, force_download = FALSE)
}
\arguments{
\item{region_pbf}{Path or URL to an \code{.osm.pbf} file. Accepts:
\itemize{
\item A full URL (e.g., \code{"https://download.geofabrik.de/south-america/brazil/nordeste-latest.osm.pbf"})
\item A Geofabrik relative path (e.g., \code{"south-america/brazil/nordeste-latest.osm.pbf"}) —
\verb{https://download.geofabrik.de/} is prepended automatically
\item A local file path — used directly if the file exists
}}

\item{max_table_size}{Maximum number of table entries for the OSRM server.
Default: \code{10000L}. Increase if computing large distance matrices.}

\item{force_download}{If \code{TRUE}, re-download the PBF file even if it exists
in the cache. Default: \code{FALSE}.}
}
\value{
The server URL (invisibly).
}
\description{
Downloads an OSM PBF file (if needed), prepares the routing graph, and starts
a local OSRM server. Configures the \code{osrm} package to use this server.
}
\details{
The PBF file is cached in \code{tools::R_user_dir("orcedata", which = "cache")}.
The OSRM graph is prepared in a temporary directory and is ephemeral.

Uses \code{\link[osrm.backend:osrm_start]{osrm.backend::osrm_start()}} which automatically installs OSRM
binaries and prepares the routing graph if needed.

If a server is already running, a warning is issued and the function returns
early without starting a new server.
}
