% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/completar_distancias.R
\name{completar_distancias}
\alias{completar_distancias}
\title{Complete a Distance Matrix via Augmented Graph Routing}
\usage{
completar_distancias(dist_df, src, dst = NULL, kmh_snap = 2, max_snap_h = 1)
}
\arguments{
\item{dist_df}{A data.frame from \code{\link[=get_distancias_osrm]{get_distancias_osrm()}} with
\code{preencher_na = FALSE}. May contain NAs for unroutable pairs. Must
have \code{.id_orig} and \code{.id_dest} columns.}

\item{src}{An \code{sf} POINT object with origin points (same as passed to
\code{\link[=get_distancias_osrm]{get_distancias_osrm()}}).}

\item{dst}{An \code{sf} POINT object with destination points. \code{NULL} for
symmetric (src-to-src) matrices.}

\item{kmh_snap}{Speed (km/h) for Euclidean and snap edges. Default: \code{2}.}

\item{max_snap_h}{Maximum snap duration (hours) before reclassifying a
point as unsnappable. Default: \code{1} (2 km at 2 km/h).}
}
\value{
A data.frame in the same format as \code{\link[=get_distancias_osrm]{get_distancias_osrm()}}, with
all NAs replaced by graph-routed durations. Includes a \code{metodo} column:
\code{"osrm"} for OSRM-routed pairs, \code{"grafo"} for graph-completed pairs.
}
\description{
Takes the output of \code{\link[=get_distancias_osrm]{get_distancias_osrm()}} (with NAs for unroutable
pairs) and completes it by building an augmented graph with road, snap,
and Euclidean bridging edges, then computing shortest paths via igraph.
}
\details{
The augmented graph connects disconnected road network components via
Euclidean edges at \code{kmh_snap} speed. Points that snapped beyond
\code{max_snap_h} hours are reclassified as unsnappable and connected via
Euclidean edges to each other and to every road component.

For graph-routed pairs, \code{distancia_km} is the Euclidean (straight-line)
distance between origin and destination, since the actual road distance
is unknown. The primary output for optimization is \code{duracao_horas}.

If snap attributes are not present on \code{dist_df} (e.g., from pre-computed
results), the graph uses original point coordinates directly and assumes
distances already account for snapping.
}
\seealso{
\code{\link[=get_distancias_osrm]{get_distancias_osrm()}} to compute the initial distance matrix.
}
